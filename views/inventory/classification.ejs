<%- include('../partials/header') %>

<main>
    <% if (title) { %>
        <h1><%= title %></h1>
    <% } else { %>
        <script>window.location.href = "/"</script>
    <% } %>

    <!-- Message Block (enable later if needed) -->
    <%# messages() %>

    <!-- Inventory Items Display -->
    <div class="inventory-grid">
        <%- grid %>
    </div>
</main>

<!-- <%- include('../partials/footer') %> -->